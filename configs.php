<?php

// define('BASEDIR', __DIR__);
define('APPDIR', join('/', ['src', 'views']));
define('ASSETSDIR', join('/', [APPDIR, 'assets'])); // Quando for definida a rota mudar pra $_REQUEST['HTTP_HOST']
